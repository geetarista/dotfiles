== About

My configuration files for bash, git, ruby, rails, vim, etc.

== Installation

WARNING: Existing files are deleted during installation.

To use, just run the following commands:

  git clone git://github.com/geetarista/dotfiles.git

  cd dotfiles

  rake install

This will create a symbolic link to each of these files in your home directory.
